# Domain log — phase-integration

Append-only. See `.codex/memory/PROTOCOL.md` for format and rules.
Recorder: parent/orchestrator via `memory-domain-sync`; routing evidence source is `.codex/agents/phase-router.toml`.
