# Shared Signal Board

Append-only cross-domain trace log for the domain-engineering agent
swarm. See `.claude/memory/PROTOCOL.md` for the entry format, tags, and
rules before writing here. Do not edit or delete prior entries; append
corrections and resolutions instead.

This board carries no authority under `CLAUDE.md`'s governing-authority
split — it is a coordination trace, not an approval, ledger event, or
active-configuration record.
