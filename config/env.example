# Safe environment template. All values are intentionally empty.
# Keep credentials outside the repository and use demo credentials only.
KALSHI_DEMO_ACCESS_KEY=
KALSHI_DEMO_PRIVATE_KEY_PATH=

# The trading process must never receive OPENROUTER_API_KEY.
